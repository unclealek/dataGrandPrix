Data Grand Prix — bug map
Backend
BUG 1 · scoring_engine.py
Normalization check requires title-case
{Soft, Medium, Hard} but LOWER() outputs lowercase
BUG 2 · scoring_engine.py
Null handling: LOWER(NULL) = NULL
tire_type NULLs survive → score never reaches 30/30
BUG 3 · scoring_engine.py
Dedup: DISTINCT * only removes exact dupes
driver+lap dupes persist if any col differs
BUG 4 · sql_engine.py
Unused SQLEngine class — GameSession handles
all logic but sql_engine.py duplicates it unsafely
BUG 5 · backend
No requirements.txt — backend deps undocumented
(fastapi, uvicorn, duckdb, pandas, numpy)
Frontend
BUG 6 · vite.config.js
No proxy configured for /api → backend
All API calls to localhost:8000 will CORS-fail
BUG 7 · index.html
Title is "frontend" not "Data Grand Prix"
favicon.svg referenced but doesn't exist
BUG 8 · SQLEditor.jsx
Monaco auto-fallback to textarea after 1.5s
even when Monaco loads fine → jarring switch
BUG 9 · App.jsx
handleRunRace requires scorecard pre-set
but /run-race already scores internally — redundant check
NOTE · frontend
No npm install instructions / README setup steps
Tailwind v4 (@tailwindcss/vite) needs no config file
Critical (blocks first run)
Moderate (degrades experience)
Minor / note


Recommended fix order
1. Fix normalization check (BUG 1+2)
2. Add vite proxy (BUG 6)
3. Fix dedup scoring (BUG 3)
4. Fix Monaco fallback (BUG 8)
5. Add requirements.txt + README (BUG 5
